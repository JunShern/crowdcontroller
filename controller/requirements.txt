# Controller dependencies (run on the machine playing the game)
websockets>=10.0   # client connection to the backend
aiohttp>=3.8.0     # serves the local visualizer page
pyautogui>=0.9.53  # sends keyboard/mouse input to the game
